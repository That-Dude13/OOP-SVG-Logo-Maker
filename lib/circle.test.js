// const Circle = require('./circle')

describe("Circle", () => {
    test("circle has fill color", () => {
      expect(Circle(fillColor)).tobe(true);
    });
  });
  
// const Circle = circle();
// Circle.fillColor("fillColor");
// expect(circle.fillColor()).toEqual('fillColor');